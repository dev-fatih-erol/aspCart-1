@model ProductModel
<div>
    @if (Model.Description != null && Model.Description.Length > 0)
    {
@Html.Raw(Model.Description) }
else
{
<h5 style="text-align: center; padding: 20px;">No description yet</h5>}
</div>